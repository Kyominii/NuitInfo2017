doctype html
html
    head
        title Dashboard
    body

      if (displayMode == 0)
        p Bienvenue #{pseudo}

        p Liste de vos véhicules :
          each item in data
          li #{item["brand"]} : #{item["capacity"]} places

        hr

        p Ajouter un véhicule

        form(action='/users/car/add',name="addVehicle", method="post")
            div.input
                span.label Marque :
                input(type="text", name="brand")
            div.input
                span.label Capacité :
                input(type="number", name="capacity")
            div.actions
                input(type="submit", value="Envoyer")
      else if(displayMode == 1)
        p Votre véhicule a été ajouté !
        a(href="/users") Retour
      else
        p Erreur de mode !
